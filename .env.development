# Development environment configuration
# ⚠️ DO NOT COMMIT THIS FILE TO PRODUCTION
# ⚠️ GENERATE YOUR OWN KEYS - NEVER USE EXAMPLE VALUES
#
# To generate secure keys, run:
# python -c "from cryptography.fernet import Fernet; import base64, secrets; print('ENCRYPTION_KEY=' + Fernet.generate_key().decode()); print('SALT_KEY=' + base64.urlsafe_b64encode(secrets.token_bytes(32)).decode()); print('PEPPER_KEY=' + base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"

# Local PostgreSQL database (Docker)
DATABASE_URL=postgresql://<DB_USER>:<DB_PASSWORD>@localhost:5432/<DB_NAME>

# Development encryption keys (generate your own!)
ENCRYPTION_KEY=<BASE64_32B_KEY>
SALT_KEY=<BASE64_32B_SALT>
PEPPER_KEY=<BASE64_32B_PEPPER>

# Discord Bot (development)
DISCORD_TOKEN=<YOUR_DEV_BOT_TOKEN>
DISCORD_GUILD_ID=<YOUR_TEST_GUILD_ID>

# Logging
LOG_LEVEL=DEBUG
LOG_FILE=logs/bot_dev.log

# Feature flags
ENABLE_DEBUG_COMMANDS=true
ENABLE_METRICS=false
